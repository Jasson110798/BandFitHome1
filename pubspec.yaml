name: bandfit_home
description: Resistance band home workout app for Android.
publish_to: 'none'
version: 1.3.0+5

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  http: ^1.2.2
  image_picker: ^1.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/exercises/
